import 'package:flutter/material.dart';

class ResidenceSettingsScreen extends StatelessWidget {
  const ResidenceSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Configurar Residência')),
      body: const Center(
        child: Text('Ecrã de Configuração de Residência em construção.'),
      ),
    );
  }
}
